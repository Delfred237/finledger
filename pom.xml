<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Coordonnées du projet -->
    <groupId>com.portfolio</groupId>
    <artifactId>finledger-cli</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>FinLedger CLI</name>
    <description>
        Personal budget management CLI built with native Java.
    </description>

    <properties>
        <!-- Encode les fichiers en UTF-8 pour éviter les problèmes d'accents -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Compile et cible Java 21 -->
        <maven.compiler.release>21</maven.compiler.release>

        <jackson.version>2.17.2</jackson.version>
    </properties>

    <!--
        Gestion centralisée des versions.
        Ici, on importe le BOM JUnit 5 pour ne pas gérer
        les versions des modules JUnit un par un.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>5.10.2</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!--
            JUnit Jupiter est l'API moderne de JUnit 5.
            Elle est uniquement utilisée pour les tests.
        -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <!--
                Plugin de compilation Maven.
                On fixe explicitement la version Java cible.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>

            <!--
                Surefire exécute les tests unitaires JUnit 5.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>

            <!--
                Jar plugin permet de produire un jar exécutable
                en précisant la classe principale.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.portfolio.finledger.Main</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!--
                Exec plugin permet de lancer l'application facilement avec Maven :
                mvn compile exec:java
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <mainClass>com.portfolio.finledger.Main</mainClass>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>